#include<iostream>
 namespace sycs4
 {
    int add()
    {
        int a,b,sum;
        std::cout<<"enter the two number";
        std::cin>>a>>b;
        std::cout<<"sum of two number is"<<(a+b);
        return 0;
    }
    int sub()
    {
        int a,b,sub;
        std::cout<<"enter the two number";
        std::cin>>a>>b;
        std::cout<<"sub of two number is"<<(a-b);
        return 0;
    }
 }