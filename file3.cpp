#include<iostream>
 namespace sycs4
 {
    int mul()
    {
        int a,b,mul;
        std::cout<<"enter the two number";
        std::cin>>a>>b;
        std::cout<<"mul of two number is"<<(a*b);
        return 0;
    }
    int div()
    {
        int a,b,div;
        std::cout<<"enter the two number";
        std::cin>>a>>b;
        std::cout<<"div of two number is"<<(a/b);
        return 0;
    }
 }